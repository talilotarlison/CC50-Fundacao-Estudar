
def main():
    for i in range(10):
        meow()

def meow()
    print("meow")

main() 
# a funcao main() funciona como um ponto de entrada para o programa, 
# e a funcao meow() imprime "meow" na tela. O loop for chama a funcao meow() 10 vezes, 
# resultando na impressao de "meow" 10 vezes. Isso demonstra como as funcoes podem ser usadas
#  para organizar o codigo e evitar repeticao. A funcao main() pode ser chamada em qualquer 
#  lugar do programa, tornando-a uma maneira conveniente de estruturar o fluxo de execucao.

# A funcao meow() pode ser chamada em qualquer lugar do programa, tornando-a uma maneira conveniente 
# de estruturar o fluxo de execucao. nao vai da erro se a funcao meow() for chamada antes de ser definida,
#  desde que a funcao main() seja chamada depois. Isso é conhecido como "forward declaration", onde a funcao pode ser chamada antes de ser definida, desde que a definicao ocorra antes da execucao do programa.
# Isso é útil para organizar o código e evitar repetições. Além disso, a função main() pode ser chamada em qualquer lugar do programa, tornando-a uma maneira conveniente de estruturar o fluxo de execução.

